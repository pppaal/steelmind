ANTHROPIC_API_KEY=sk-ant-...
SENSOR_HZ=20
# Set to 1 only when behind a trusted reverse proxy that sets X-Forwarded-For,
# so per-client AI rate limiting keys off the real client IP instead of the
# proxy. Leave off otherwise — the header is client-spoofable.
TRUST_PROXY_HEADERS=0
